#include<stdio.h>
int main(){
	int num,rem,res=0,tmp,sum;
	printf("enter the number: ");
	scanf("%d",&num);
	tmp=num;
	while(num>0){
		rem=num%10;
		res+=rem*rem*rem;
		num=num/10;
	}
	if(tmp==res){
		printf("it is armstrong");
	}
	else{
		printf("it is not armstrong");
	}
}
