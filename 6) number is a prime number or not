#include<stdio.h>
int main(){
	int num,i,count;
	printf("enter the number: ");
	scanf("%d",&num);
	for(i=2;i<=num;i++){
		if(num%i==0)
		{
		count++;
	}
	}
	if(count>2){
		printf("%d is a not prime number",num);
	}
	else{
		printf("%d is a prime number",num);
	}
}
